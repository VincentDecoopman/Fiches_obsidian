L'app doit parler au serveur (backend) pour pouvoir recevoir ou envoyer des données (protocole HTTP)

Rest = Representation State Transfer

Le serveur reçoit des requêtes (CRUD) :
Create, Read, Update, Delete.

