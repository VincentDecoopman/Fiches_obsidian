Build une image 
```DOCKERFILE
docker build -t <nom du conteneur a creer> .
```

Lister les images build : 
```Dockerfile
docker images
```

Lancer le docker en question : 
```Dockerfile
docker run -it <nom du conteneur créer> /bin/bash
```

